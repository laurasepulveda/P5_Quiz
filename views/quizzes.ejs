<!DOCTYPE html>
<html>
  <head>
    <title>Quizzes</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <a href="/">Inicio</a>
    <h1>Quizzes: </h1>
<ul>
    <% for (var i in quizzes) { %>
    <% var quiz = quizzes[i] %>
      <li>
            <%= quiz.question%> : <%= quiz.answer %>
      </li>
    <% } %>
</ul>
  </body>
</html>

<!-- Instrucciones de control usar <% %> , <%= %> ejecuta el javascript pero no lo muestra -->